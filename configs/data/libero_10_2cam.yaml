defaults:
  - libero_2cam
  - _self_

train:
  dataset_name: libero_10
  dataset_dirs:
    - ./data/libero_mujoco3.3.2/libero_10_no_noops_lerobot
  pretrained_norm_stats: null
  dream_target:
    enabled: true
    mode: fixed_offset
    future_offset: 32
    modalities: ["dyn", "depth", "dino", "sam"]
    depth:
      source: depth_anything
      root: ./data/libero_mujoco3.3.2/libero_10_no_noops_lerobot/extras/depth_anything_v3_metric
      model: depth_anything_v3
